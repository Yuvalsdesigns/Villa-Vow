{
  "name": "villa-vow-planner-worker",
  "description": "Cloudflare Worker for Villa & Vow's planner assistant — verifies the caller's Firebase login, then calls the free Gemini API with the key kept as a Worker secret.",
  "private": true,
  "dependencies": {
    "jose": "^5.9.6"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
